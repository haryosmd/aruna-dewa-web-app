node_modules
**/node_modules
**/.nuxt
**/.output
**/dist
**/.turbo
.turbo
docs
.git
.github
.claude
.codex
.data
**/.env
**/.env.*
!**/.env.example
test-results
playwright-report
tests
playwright.config.ts
# Bentuk produksi dijalankan dari server, bukan dari dalam image.
compose.yaml
compose.prod.yaml
Caddyfile
# Runbook dan skrip operasi dijalankan dari laptop atau dari server, tidak pernah dari dalam
# image aplikasi.
ops
# `packs/` adalah SUMBER ornamen, 31 MB, dan hanya dibaca scripts/ornament-* di laptop.
# Komponen Vue-nya sudah digenerate dan ikut git di apps/web/components/ornament, jadi tidak
# ada satu pun jalur build maupun runtime yang membuka direktori ini.
packs
